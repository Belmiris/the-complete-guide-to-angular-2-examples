import { BindingHighlightDirective } from './binding-highlight.directive';

describe('BindingHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new BindingHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
